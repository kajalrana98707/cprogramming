#include <stdio.h>

int main()
{
	int side= 14;
	int perimeter;
	int area;
    perimeter = 4*side;
    area = (side*side);
	printf("perimeter of square = %d inches\n",perimeter);
	printf("Area of square = %d square inches",area);
		return 0;
}
